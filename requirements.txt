# Serverless (Vercel) runtime deps — the lean subset the API + cron jobs import.
# No prefect / polars / kafka / eccodes / herbie (all container-only), and no
# boto3 (add it only if USE_LOCAL_BRONZE=false, i.e. writing bronze to S3).
fastapi>=0.115
psycopg[binary,pool]>=3.2
httpx>=0.27
pydantic-settings>=2.6
pyyaml>=6.0
numpy>=1.26
scikit-learn>=1.5
lightgbm>=4.5
joblib>=1.4
