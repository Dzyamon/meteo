# Vercel (serving) deps ONLY — the API reads the DB and serves JSON/HTML, so it
# needs no ML stack. The heavy fetch/train pipeline runs on GitHub Actions
# (.github/workflows/pipeline.yml), not here, which keeps this bundle tiny and
# well under Vercel's function-size limit.
fastapi>=0.115
psycopg[binary,pool]>=3.2
pydantic-settings>=2.6
pyyaml>=6.0
httpx>=0.27
